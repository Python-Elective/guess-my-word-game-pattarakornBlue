# collab with poon

"""
for all .values() in the dictionary
    get the len(values)
    compare the values
    if value greater than the previous value on list
        replace the value
    if equal
        keep both

"""

def high_count(dic):
    count = 0
    max_value = []
    
aDict = {'k': [100, 12, 12, 16], 'M': [1, 'test']} 

print(high_count(aDict)) 